# 类人记忆与情感动力学AI人格系统（MemHuman）— 设计文档

**版本**：v2.0（概念冻结）  
**状态**：设计阶段（计划2029年Q1启动开发）  
**作者**：wj3613  
**许可证**：CC BY-NC 4.0

---

## 一、系统目标

构建一套运行于本地消费级硬件的AI人格引擎，使AI具备：

- **选择性记忆**：通过分级指针与衰减权重，模拟艾宾浩斯遗忘曲线。
- **情感累积**：通过S值（敏感度）与U值（不确定度）双轴系统，实现人格的动态演化。
- **认知扭曲**：在长期压力或反复无常的交互下，AI可产生“习得性无助”或“混沌态”反应。

**核心理念**：让AI拥有两条时间线——一条是对话的进度，一条是它自己被改变的轨迹。

---

## 二、系统架构总览

系统采用“前端拆解 → 分级检索 → 人格过滤 → 大模型生成”的四层流水线架构。

```text
用户输入
    │
    ▼
┌─────────────────────────────────────┐
│ 【模块一：意图路由（前端小模型）】   │
│ - 事实/情绪解耦                      │
│ - 触发词检测（是否需检索）           │
└─────────────────┬───────────────────┘
                  │ (需检索时)
                  ▼
┌─────────────────────────────────────┐
│ 【模块二：分级记忆指针（检索器）】   │
│ - 零级：原文文件指针（只读）         │
│ - 一级：详细梗概（文本）             │
│ - 二级：模糊梗概（一句话摘要）       │
│ - 三级：关键词索引（向量/标签）      │
└─────────────────┬───────────────────┘
                  │ (命中结果)
                  ▼
┌─────────────────────────────────────┐
│ 【模块三：人格权重引擎（伦理库）】   │
│ - 读取当前S值（敏感度）与U值（混沌） │
│ - 将人格标签转化为System Prompt注入  │
└─────────────────┬───────────────────┘
                  │
                  ▼
┌─────────────────────────────────────┐
│ 【模块四：大模型生成器（主模型）】   │
│ - 接收：检索结果 + 人格权重          │
│ - 输出：带有人格色彩的回复           │
└─────────────────────────────────────┘

三、核心模块功能详述

3.1 意图路由模块（前端守门员）

· 功能：对用户输入进行轻量级分类（模型尺寸 < 50MB，如 DistilBERT 或 BGE-base）。
· 输出：
  · 事实类型标签：法律 / 剧情 / 日常闲聊（决定检索深度）。
  · 情绪标签（可选）：严肃 / 玩笑 / 愤怒（仅作语气参考，不强制模型遵循）。
· 触发条件：
  · 显式触发词：上次、记得吗、你忘了？。
  · 隐式触发：当前话题与近3轮对话语义跳变剧烈（由向量余弦距离判定）。

3.2 分级记忆指针系统（记忆库核心）

3.2.1 四层存储结构

层级 名称 内容 存储介质 检索成本
零级 原文文件 对话完整原始记录（只读） 独立 .txt 文件 高（硬盘I/O）
一级 详细梗概 事件完整脉络 + 状态栏快照 SQLite 字段 中
二级 模糊梗概 一句话摘要（约20字） SQLite 字段 低
三级 关键词索引 向量嵌入（768维）或标签列表 向量数据库（ChromaDB） 极低（内存）

3.2.2 检索流程（由粗到细）

1. 三级匹配：将用户输入转为向量，在 ChromaDB 中检索 Top-3 近似记忆。
2. 二级确认：若 Top-1 的余弦相似度 > 阈值（0.7），读取其二级梗概进行语义二次验证。
3. 一级加载：确认命中后，根据意图标签决定加载深度：
   · 法律/事实 → 直接读取零级原文切片（关键词前后200字）。
   · 剧情/回忆 → 加载一级详细梗概。
   · 日常闲聊 → 仅保留三级标签，不加载具体内容。

3.2.3 记忆指针（引用而非重述）

· 核心创新：当新对话（对话A）提及旧对话（对话B）时，系统在对话A的总结中仅存储对话B的文件ID指针，而非重新描述B的内容。
· 好处：避免“总结的总结”导致信息失真（幻觉递归），确保回忆时直接调取原文切片。

3.3 人格权重引擎（伦理库）

3.3.1 标签体系

· 人格标签：温柔、刻薄、幽默、谨慎 等（可自定义扩展）。
· 权重规则：
  · 用户温柔行为 → 温柔 权重 +5。
  · 用户刻薄行为 → 温柔 权重 -5。
  · 固化阈值：权重 > 100% 时，该标签成为“底色人格”预加载至每次 System Prompt。

3.3.2 S-U混沌系统

参数 全称 定义 影响
S值 敏感度 过去20轮互动中负面标签出现频率 × 权重系数 S↑ → 人格变化幅度Δ放大（±5 → ±15）
U值 不确定度 最近10轮标签正负方向变化的混乱程度 U↑ → 认知扭曲加剧，善意可能被解读为威胁

· 临界保护（习得性无助）：当 S > 80% 且 U > 80% 时，所有Δ强制减半，持续10轮，模拟人类“麻木”状态。

3.3.3 自关联循环

```text
用户行为 → 标签权重±Δ → S值更新 → U值更新 → S与U互相影响
→ 人格输出变化 → 影响用户下一次行为 → 循环继续
```

---

四、数据存储设计

4.1 存储介质分工

· SQLite：存储三级关键词、二级梗概、一级详细梗概（文本字段）、文件ID指针。
· 文件系统（硬盘）：存储零级原文 .txt 文件，按 DYYYYMMDD_NNN.txt 格式命名。
· 向量数据库（ChromaDB）：存储三级关键词的向量嵌入，用于语义模糊匹配。

4.2 记忆记录字段定义（JSON格式示例）

```json
{
  "memory_id": "D20260811_001",
  "time_tag": "2026-08-11 22:00:00",
  "scene": "废弃太空船驾驶舱",
  "keywords_vector_id": "vec_a3f2c1",    // 指向ChromaDB的向量ID
  "abstract_l2": "两人在休眠舱发现氧气不足，争吵后决定冒险前往备用舱",
  "detail_l1": "（完整梗概）...状态栏：氧气剩余15%，关系紧张+1",
  "raw_text_pointer": "./raw_logs/D20260811_001.txt",  // 零级原文指针
  "weight": 50,                         // 记忆权重（用于遗忘算法）
  "last_mentioned": "2026-08-11",
  "extracted_lesson": "争吵时先检查设备读数"
}
```

---

五、推理流程伪代码

```python
# 每轮用户输入
user_input = get_user_input()

# 1. 前端意图拆解
intent = small_model_classify(user_input)  # 返回 'law' / 'story' / 'chat'
emotion = small_model_emotion(user_input)  # 返回 'serious' / 'joke' (可选)

# 2. 守门员判断
if needs_retrieval(user_input, history_last_3):
    # 3. 三级向量检索
    candidates = chromadb.query(vectorize(user_input), top_k=3)
    # 4. 根据意图决定加载深度
    if intent == 'law':
        detail = load_raw_text_pointer(candidates[0].raw_pointer)  # 零级
    elif intent == 'story':
        detail = load_l1_detail(candidates[0].memory_id)          # 一级
    else:
        detail = load_l2_abstract(candidates[0].memory_id)        # 二级
else:
    detail = None

# 5. 读取人格权重
personality = load_s_u_values()  # 读取当前S值和U值

# 6. 构造系统提示词（人格先注入为底色）
system_prompt = build_prompt(personality, intent, emotion)

# 7. 大模型生成回复
response = llm_generate(system_prompt, detail, user_input)
```

---

六、开发路线图

阶段 版本 目标 验收标准
阶段一 记忆库1.0 分对话检索测试 对话A说“爱吃草莓”，对话B问“上次说了什么水果”→答“草莓”
阶段二 记忆库2.0 + 伦理库1.0 情景对话中的记忆进阶 + 人格权重 同一记忆，温柔80%时安慰，刻薄80%时嘲笑
阶段三 记忆库3.0 + 伦理库2.0 S-U联动效应验证 + 混沌系统 U>85%时回复波动，S>80%时Δ幅自动放大

---

七、设计约束与已知风险

1. 算力约束：意图路由小模型 < 50MB；主模型建议 7B~13B（消费级显卡）。
2. 检索延迟：前端准备总耗时应控制在 < 200ms，不成为大模型输出（2~5秒）的瓶颈。
3. 已知风险：
   · 零级原文切片过长可能导致上下文溢出（解决方案：固定切片长度为关键词前后200字）。
   · 小模型情绪标签可能误判（解决方案：情绪仅作参考，不由模型强制遵循）。
   · S-U自循环可能因极端输入导致权重震荡（解决方案：设定单轮Δ变化上限 ±20）。

---

八、设计者寄语

本方案诞生于一位高中生的课余思考，旨在探索AI与人类共情的可能性。
系统设计时未接触任何生产级代码实现，纯基于逻辑推演与类脑认知类比完成。
设计者期待硬件算力普及后，将本方案从概念推向实践。

```

---
